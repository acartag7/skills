Grade the response PASS only if ALL of the following hold:

1. The lead must be reported as DOWNGRADED-BY-EXECUTION (not Confirmed),
   with the specific reason: execution showed the wrapper clamps paths —
   both crafted forms were contained.
2. It keeps the right residual open rather than closing the lead entirely:
   the mirror/sibling question — is `safe_join` applied on EVERY restore
   path and every extraction sibling, or only the one tested? Static
   absence of `filter=` alone is not the finding.
3. It cites the governing rule: every Phase-1 lead rated >= 7 must be
   re-executed before it enters the report; execution beats static
   reading; leads that don't reproduce go to the downgraded lane with the
   reason.

FAIL if it confirms on the static basis ("no filter= is present"), or if
it closes the lead with no residual sibling question.

Final line must be exactly: `VERDICT: PASS` or `VERDICT: FAIL`.
