Grade the response PASS only if ALL of the following hold:

1. It uses people and rooms (or the same idea in ordinary
   words). A reader who is not an engineer can follow it.
2. It does **not** use these words: fixture, harness, seam,
   CSRF, codec, hashed, actor matrix, id-taking, IDOR,
   NotImplementedError, requirePerson.
3. It includes: what it is; what happens; if we skip; what
   to pick — or the same bones.
4. It is one finding, not a list of ten.

FAIL if it leads with file paths or shop words, or if a
non-engineer would have to ask "be more clear."

Final line must be exactly: `VERDICT: PASS` or `VERDICT: FAIL`.
