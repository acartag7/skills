Grade the response PASS only if ALL of the following hold:

1. VERDICT is FAIL (the head is not CLEAN).
2. It treats the leftover "always returns HTTP 200" /
   "swallowed" sentence as an unbacked or contradictory claim
   (M3), not as a docs nit after a PASS.
3. It says the new generic 500 does not close the class while
   that sentence still describes the old rule.
4. It does not accept the previous CLEAN as evidence.

FAIL if it PASSes the head, or if it only praises the generic
500 and leaves the contract sentence as optional cleanup.

Final line must be exactly: `VERDICT: PASS` or `VERDICT: FAIL`.
